/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['dirty-snow-panel.local.com', 'dirty-snow-admin.hot-rice.com', 'admin-dirty-snow.warm-rice.dev'],
  },
}

module.exports = nextConfig
